# Discord Bot Configuration
DISCORD_BOT_TOKEN=your_discord_bot_token_here
TASK_CHANNEL=1234567890               # Channel ID where task board displays
DASHBOARD_CHANNEL=1234567890          # Read-only dashboard channel for high-level stats
TASK_FORUM_CHANNEL=2233445566         # Forum channel ID (one thread per task)
REMINDER_CHANNEL=1122334455          # Channel ID for deadline reminders

# Task-Master Configuration
TASKMASTER_USERNAME=gelvey  # Global username for database access (which account to use)
OWNERS=Circuit Gelvey       # Space-separated list of possible task assignees

# User Mapping (Discord User ID -> Task Owner from OWNERS list)
# Format: DISCORD_USER_<discord_user_id>=Task-Owner-Name
# These map Discord users to the OWNERS above for task assignment
DISCORD_USER_123456789012345678=Circuit
DISCORD_USER_987654321098765432=Gelvey
# Add more mappings as needed

# Firebase Configuration (Local to Discord Bot)
# Configure these for the Discord bot's Firebase connection
# This can be the same Firebase instance as the web app, or a separate one
# Priority: 1) Environment variables below, 2) discord_bot/credentials.json, 3) ../credentials.json
FIREBASE_DATABASE_URL=https://your-project-id.firebasedatabase.app/
FIREBASE_TYPE=service_account
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY_ID=your-private-key-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_CLIENT_ID=your-client-id
FIREBASE_AUTH_URI=https://accounts.google.com/o/oauth2/auth
FIREBASE_TOKEN_URI=https://oauth2.googleapis.com/token
FIREBASE_AUTH_PROVIDER_CERT_URL=https://www.googleapis.com/oauth2/v1/certs
FIREBASE_CLIENT_CERT_URL=https://www.googleapis.com/robot/v1/metadata/x509/...

# Alternative: Place credentials.json file in discord_bot/ directory
# The bot will look for credentials in this order:
# 1. Environment variables (above)
# 2. discord_bot/credentials.json (local to bot)
# 3. ../credentials.json (parent directory, shared with web app)

# Optional: Local storage fallback (if Firebase is not available)
USE_LOCAL_STORAGE=false

# Task Refresh Settings
TASK_BOARD_REFRESH_INTERVAL=60  # Seconds between task board updates
REMINDER_CHECK_INTERVAL=300     # Seconds between reminder checks (5 minutes)

# Logging
LOG_LEVEL=INFO
LOG_FILE=discord_bot.log
